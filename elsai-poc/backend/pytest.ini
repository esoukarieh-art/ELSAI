[pytest]
testpaths = tests
addopts = -ra --strict-markers
markers =
    fp: faux positif connu de safety.scan — documente un gap à durcir
    fn: faux négatif connu de safety.scan — documente un gap à durcir
    adversarial: red-team safety (jailbreak / evasion / injection)
